//Dependencies
const express = require('express');

//Port
//Route

//Start server
const app = express();



//listen
app.listen(PORT, () => {
    console.log(`API server is ready on port ${PORT}!`);
});